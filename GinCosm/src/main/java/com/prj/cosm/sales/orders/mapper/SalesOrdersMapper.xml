<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.prj.cosm.sales.orders.mapper.SalesOrderMapper">
	<!-- 주문번호순으로 정렬 - public int getOrderNo(); -->
	<select id="getOrderNo" resultType="SalesOrderVO">
	SELECT NVL(MAX(order_no), 0) + 1 as orderNo FROM orders
	</select>
	
	<!-- 주문번호순으로 정렬 -->
	<select id="salesOrderList" resultType="SalesOrderVO">
	select * from orders ORDER BY order_no
	</select>
	
	
	<!-- 주문 전체조회 -->
	
	
	<!-- 주문 단건조회 -->
	
	
	<!-- 주문 등록 -->
	
	
	<!-- 주문 내역삭제 -->
	
  </mapper>